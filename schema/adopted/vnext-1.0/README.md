# Adopted minimum Canonical contract

> `PRE_PUBLICATION_CANDIDATE / NOT_PUBLISHED`

このdirectoryの4 artifactは、Owner Decision `OD-SAKU-PR7-20260822-01`で採択されたexact bytesです。

artifact内部に残る`candidate`／`NOT_ADOPTED`表現は採択前のimmutable bytesに由来します。現在の採択状態は同directoryの`ADOPTION_STATUS.json`がauthoritative companionとして示します。

```text
embedded candidate label != current adoption state
adoption status companion != semantic byte mutation
Canonical adoption != publication
```

This newly authored publication document is `CC BY 4.0` only at this exact publication-manifest path. It does not change the Apache-2.0 scope of the adopted exact four.
