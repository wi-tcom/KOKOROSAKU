{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimum capability set for the local-only SAKU desktop host.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
